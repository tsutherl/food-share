import {connect} from 'react-redux'
import Offering from './Offering'

// const mapStateToProps = function(state) {
//   return {
//     offerings: state.message
//   };

// };

export default connect(
      null,
     null)
(Offering)
